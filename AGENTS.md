# Local section patterns

Reusable section demos for the local websites live in `../section-library`. Check its `index.html`, `catalog.json` and the pattern guide before adapting a section. Replace the demo's image and content slots with this site's own assets and copy.
